#!/usr/bin/node
// Creates an empty class Rectangle using the class notation.

class Rectangle {
  constructor() {
    /*
       constructor is empty so when an instance of a rectangle is created
       it will have an empty object representation.
    */
  }
}

module.exports = Rectangle;
